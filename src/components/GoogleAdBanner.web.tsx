import { View } from 'react-native';

export function GoogleAdBanner() {
  // The current iPhone experience is the web/PWA build. AdSense will be wired here after the public site is deployed and approved.
  return <View />;
}
